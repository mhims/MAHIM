import { BlogPost } from '../types';

/**
 * All articles written by Mahim from the Admin Panel will be saved here automatically.
 * Kept completely empty by default so only your own articles will exist and be deployed.
 */
export const USER_BLOG_POSTS: BlogPost[] = [];
